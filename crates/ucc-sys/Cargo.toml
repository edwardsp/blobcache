[package]
name = "ucc-sys"
version = "0.1.0"
edition = "2021"
description = "Minimal raw FFI bindings to libucc (Unified Collective Communication)"
publish = false
links = "ucc"

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"
