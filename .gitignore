/target
**/target/
crates/*/Cargo.lock
**/*.rs.bk
Cargo.lock.tmp
.DS_Store
/reference
*.swp
.envrc.local
*.log

# Local environment overrides — never committed.
# See .env.template for the canonical list of variables.
.env
.env.local

# Benchmark harness artifacts (per-run pod configs, prom snapshots).
# Kept locally for provenance; not part of the redistributable repo.
bench/results
bench/results-*
bench/sweep
bench/sweep-*
bench/sweep_*.out
bench/sweep_workers*.out
bench/deploy-configs
benchmarks/out
docs/INTEGRATION_PLAN.md

night-progress.md

# Local-only review artifacts. opus_code_eval.md is the input review document
# (kept local, never committed). overview.md is local Marp slide content.
opus_code_eval.md
overview.md
