class AsyncFileWriter:  # raising stub — see tensornvme/__init__.py
    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "tensornvme stub (pyshim): async checkpoint IO is intentionally "
            "disabled for the blobcache benchmark (epochs=1, ckpt_every=100000)."
        )
